# Production API URL — set this in the Vercel dashboard as an environment variable
# Replace with your actual Railway deployment URL after deploying the backend
VITE_API_URL=https://REPLACE_WITH_RAILWAY_URL/api
